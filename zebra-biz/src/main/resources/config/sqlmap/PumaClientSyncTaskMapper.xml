<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dianping.zebra.admin.dao.PumaClientSyncTaskMapper">

    <select id="findAllByRuleName" parameterType="map"
            resultType="PumaClientSyncTaskEntity">
        SELECT * FROM PumaTask WHERE ruleName = #{ruleName}
    </select>

    <select id="findEffectiveTaskByExecutor" parameterType="map"
            resultType="PumaClientSyncTaskEntity">
        SELECT * FROM PumaTask WHERE executor = #{executor} and
        status = 2;
    </select>

    <select id="findEffectiveTaskByRuleName" parameterType="map"
            resultType="PumaClientSyncTaskEntity">
        SELECT * FROM PumaTask WHERE ruleName = #{ruleName} and
        status = 2;
    </select>
    
    <update id="updateSequence" parameterType="PumaClientSyncTaskEntity">
        UPDATE `zebra`.`PumaTask`
        SET
        sequence=#{sequence},
        WHERE id = #{id}
    </update>

    <insert id="insertSyncTask" parameterType="PumaClientSyncTaskEntity">
        INSERT INTO
        `zebra`.`PumaTask`
        (
        `ruleName`,
        `tableName`,
        `pk`,
        `dbRule`,
        `dbIndexes`,
        `tbRule`,
        `tbSuffix`,
        `pumaTaskName`,
        `pumaDatabase`,
        `pumaTables`,
        `executor`,
        `executor1`,
        `executor2`,
        `status`,
        `sequence`,
        `createTime`,
        `updateTime`
        )
        VALUES
        (
        #{ruleName},
        #{tableName},
        #{pk},
        #{dbRule},
        #{dbIndexes},
        #{tbRule},
        #{tbSuffix},
        #{pumaTaskName},
        #{pumaDatabase},
        #{pumaTables},
        #{executor},
        #{executor1},
        #{executor2},
        #{status},
        #{sequence},
        NOW(),
        NOW()
        )
    </insert>

    <update id="updateSyncTask" parameterType="PumaClientSyncTaskEntity">
        UPDATE
        `zebra`.`PumaTask`
        SET
        ruleName=#{ruleName},
        tableName=#{tableName},
        pk=#{pk},
        dbRule=#{dbRule},
        dbIndexes=#{dbIndexes},
        tbSuffix=#{tbSuffix},
        pumaTaskName=#{pumaTaskName},
        pumaDatabase=#{pumaDatabase},
        pumaTables=#{pumaTables},
        executor=#{executor},
        executor1=#{executor1},
        executor2=#{executor2},
        status=#{status},
        sequence=#{sequence},
        updateTime=NOW()
        WHERE pumaTaskName = #{pumaTaskName}
    </update>

    <update id="updateSyncTaskStatus" parameterType="map">
        UPDATE
        `zebra`.`PumaTask` SET
        status=#{status},
        updateTime=NOW()
        WHERE pumaTaskName=#{pumaTaskName}
    </update>
</mapper>