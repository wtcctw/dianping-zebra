<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dianping.zebra.biz.dao.ShardDumpDbMapper">
    <select id="getDbByName" parameterType="map" resultType="ShardDumpDbEntity">
        SELECT * FROM ShardDumpDb WHERE Name = #{name}
    </select>

    <select id="getAll" resultType="ShardDumpDbEntity">
        SELECT * FROM ShardDumpDb
    </select>

    <delete id="delete" parameterType="map">
        DELETE FROM ShardDumpDb WHERE Id = #{id}
    </delete>

    <insert id="create" parameterType="ShardDumpDbEntity"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT ShardDumpDb
        (Name,Host,Port,Username,Password)
        VALUES
        (#{name},#{host},#{port},#{username},#{password})
    </insert>
</mapper>